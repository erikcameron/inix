defmodule Inix.Exec.Mode.Session do
  @moduledoc """
  An interactive, or at least multi-step, exchange. Compare jobs,
  which are specified up front and are complete by the time they
  return to the caller.
  """
end
